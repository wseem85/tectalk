import { Lato } from 'next/font/google';
import { Tajawal } from 'next/font/google';
export const lato = Lato({ subsets: ['latin'], weight: '400' });
export const tajawal = Tajawal({ subsets: ['latin', 'arabic'], weight: '400' });
